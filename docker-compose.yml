include:
  - path: ../syfthub/deploy/docker-compose.dev.yml

services:
  # --- Syft Space ---
  space:
    build:
      context: ../syft-space
      dockerfile: Dockerfile
    environment:
      SYFTBOX_ASSIGNED_PORT: "8081"
    ports:
      - "8081:8081"
